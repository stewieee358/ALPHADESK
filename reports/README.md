# Generated reports

`RPT` writes HTML reports to this directory. Generated reports stay local and are
excluded from Git because they contain dated market data and analysis.

Run the application, select a security such as `AAPL US Equity`, and execute
`RPT` to generate a fresh report. Existing local HTML files are preserved.
